<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="personalHistory">
<select id="userList" resultType="hashMap" parameterType="hashMap">
select user_idx,user_name,user_dept,user_register_date
from user_info where user_idx between ${prevLimit} and ${dataSize};
</select>
</mapper>